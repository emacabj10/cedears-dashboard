name: Update quotes (intradiario)

on:
  schedule:
    # Lun-Vie, cada 10 minutos en horario de mercado ARG (13:40–19:20 UTC)
    # Los crons de :30 están en el workflow diario — estos cubren el resto
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 11 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 12 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 13 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 14 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 15 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 16 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 17 * * 0-6'
    - cron: '0,5,10,15,20,25,30,35,40,45,50,55 18 * * 0-6'
  workflow_dispatch:
    inputs:
      intraday_check:
        description: 'Forzar chequeo intradiario manual (dejar vacío o 1)'
        required: false
        default: '1'

jobs:
  intraday:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.11'

      - name: Intraday price check
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          INTRADAY_CHECK: "1"
        run: python alert_quotes.py

      - name: Commit data.json after intraday check
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash
          git add data.json
          git diff --staged --quiet || git commit -m "Intraday check $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
